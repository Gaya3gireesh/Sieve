# Frontend Environment Variables Example

# ⚠️  IMPORTANT: Vite only exposes variables prefixed with VITE_

# Backend API connection
# For local development: http://localhost:3001
# For production: https://sieve-quwb.onrender.com
VITE_API_BASE=http://localhost:3001
VITE_API_TARGET=http://localhost:3001

# GitHub OAuth (get from GitHub App settings)
# These are frontend-safe secrets (not used in frontend code yet, but for reference)
# GITHUB_OAUTH_CLIENT_ID=your_client_id_here
# GITHUB_OAUTH_CLIENT_SECRET=your_client_secret_here
